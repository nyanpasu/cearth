CMAKE_MINIMUM_REQUIRED (VERSION 2.8)
PROJECT (cearth) 

ADD_DEFINITIONS(
        -std=c11
        # Other flags
        )

SET (CEARTH_VERSION_MAJOR 0)
SET (CEARTH_VERSION_MINOR 1)
SET (CEARTH_VERSION_PATCH 0)

ADD_SUBDIRECTORY (src)
