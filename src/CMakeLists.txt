CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
 
PROJECT(CEARTH)
 
ADD_EXECUTABLE(cearth main.c)
 
INCLUDE(FindPkgConfig)
PKG_SEARCH_MODULE(SDL2 REQUIRED sdl2)
INCLUDE_DIRECTORIES(${SDL2_INCLUDE_DIRS})
TARGET_LINK_LIBRARIES(cearth ${SDL2_LIBRARIES})
